name: zakki-infra
runtime: nodejs
# description は stack タグにも入り、上限 256 バイト（日本語は 1 文字 3 バイト）
# を超えると `pulumi stack init` が弾かれるため短く保つ。詳細は README を参照。
description: >-
  zakki の Cloudflare リソース（apps/api の Worker と公開設定）。
  Turso はアプリ側で作るため管理外（issue #129 / #132）。
